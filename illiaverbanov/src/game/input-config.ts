export const ACTION_KEYS: ReadonlySet<string> = new Set(['KeyE', 'Space', 'Enter'])

export const CLOSE_KEYS: ReadonlySet<string> = new Set(['Escape'])

export const ADVANCE_KEYS: ReadonlySet<string> = new Set(['Space', 'Enter', 'KeyE'])

export const META_KEYS: ReadonlySet<string> = new Set(['Tab', 'Shift', 'Control', 'Alt', 'Meta'])

export const DPAD_VIBRATION_MS = 6
export const ACTION_VIBRATION_MS = 10
